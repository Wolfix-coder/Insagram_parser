# 📸 Instagram Parser

Потужна та зручна програма для завантаження контенту з Instagram профілів. Поєднує функціональність бібліотеки **Instaloader** з інтуїтивним графічним інтерфейсом на базі **CustomTkinter**.

---

## ✨ Основні можливості

- 📥 **Завантаження контенту** - Завантажуйте фотографії та відео з Instagram профілів
- ⚙️ **Гнучкі налаштування** - Налаштуйте параметри завантаження через GUI
- 📊 **Логування** - Детальне логування всіх операцій для відстеження процесу
- 🎨 **Сучасний інтерфейс** - Красивий та зручний графічний інтерфейс на CustomTkinter
- 📁 **Управління завантаженнями** - Вибір папки призначення та кількості постів
- 🔧 **JSON конфігурація** - Легко налаштовуються параметри через JSON файли

---

## 🛠️ Технологічний стек

- **Python 3.8+**
- **Instaloader** - Завантаження контенту з Instagram
- **CustomTkinter** - Сучасний GUI фреймворк
- **JSON** - Конфігурація проекту

---

## 📋 Вимоги

\`\`\`
instaloader>=4.9.0
customtkinter>=5.0.0
\`\`\`

---

## 🚀 Встановлення

### 1. Клонування репозиторію

\`\`\`bash
git clone <repository-url>
cd parser
\`\`\`

### 2. Встановлення залежностей

\`\`\`bash
pip install -r requirements.txt
\`\`\`

Або встановіть вручну:

\`\`\`bash
pip install instaloader customtkinter
\`\`\`

### 3. Запуск програми

\`\`\`bash
python src/main.py
\`\`\`

---

## 📁 Структура проекту

\`\`\`
parser/
├── assets/                          # Ресурси проекту
│   └── font/                        # Шрифти
│       ├── Inter.ttf
│       ├── NotoSans.ttf
│       └── Roboto.ttf
├── config/                          # Конфігурація
│   ├── core_settings.json           # Параметри ядра (Instaloader)
│   ├── gui_settings.json            # Параметри GUI
│   └── settings.py                  # Завантажувач конфігурацій
├── data/                            # Дані проекту
│   └── input/                       # Вхідні дані
├── src/                             # Основний код
│   ├── main.py                      # Точка входу програми
│   ├── gui/                         # Графічний інтерфейс
│   │   ├── app.py                   # Головна програма GUI
│   │   └── windows/                 # Вікна програми
│   │       ├── main_window/         # Головне вікно
│   │       └── option_window/       # Вікно налаштувань
│   ├── insta_parser/                # Ядро парсера
│   │   └── core.py                  # Основна логіка завантаження
│   └── utils/                       # Утиліти
│       └── logging.py               # Система логування
└── README.md                        # Цей файл
\`\`\`

---

## ⚙️ Конфігурація

### core_settings.json

Параметри завантаження контенту:

\`\`\`json
{
    "instaloader": {
        "download_pictures": true,           // Завантажувати фотографії
        "download_videos": false,            // Завантажувати відео
        "download_video_thumbnails": false,  // Завантажувати превью відео
        "save_metadata": false,              // Зберігати метадані
        "post_metadata_txt_pattern": "",     // Шаблон метаданих
        "quiet": false,                      // Тихий режим
        "sanitize_paths": true               // Очищення шляхів
    },
    "download": {
        "username": "asttea_f",              // Юзернейм профілю
        "count": 2,                          // Кількість постів для завантаження
        "path": "C:/Users/dragon/Desktop"    // Папка призначення
    }
}
\`\`\`

### gui_settings.json

Параметри графічного інтерфейсу:

\`\`\`json
{
    "window": {
        "width": 600,
        "height": 400,
        "title": "Instagram Parser v1.0"
    },
    "theme": {
        "bg_color": "#ffffff"
    },
    "font": {
        "font_1": {"path": "/assets/font/Inter.ttf", "size": 15},
        "font_2": {"path": "/assets/font/NotoSans.ttf", "size": 15},
        "font_3": {"path": "/assets/font/Roboto.ttf", "size": 15}
    }
}
\`\`\`

---

## 🎯 Використання

### Запуск програми

\`\`\`bash
python src/main.py
\`\`\`

### Основні кроки:

1. **Запустіть програму** - Відкриється головне вікно
2. **Введіть юзернейм** - Вкажіть Instagram профіль для завантаження
3. **Виберіть папку** - Оберіть місце збереження файлів
4. **Налаштуйте параметри** - Відкрийте вікно налаштувань (кнопка "Options")
5. **Почніть завантаження** - Натисніть кнопку "Download"
6. **Спостерігайте прогрес** - Логи показуватимуть хід процесу

---

## 📝 Основні компоненти

### InstaApp (src/insta_parser/core.py)

Ядро програми, яке керує завантаженням контенту:

\`\`\`python
core = InstaApp()
core.download(username="asttea_f", limit=10, target_dir="./downloads")
\`\`\`

**Методи:**
- \`load_config()\` - Завантажує конфігурацію
- \`init_loader()\` - Ініціалізує Instaloader
- \`download(username, limit, target_dir)\` - Завантажує контент
- \`reload_config()\` - Перезавантажує конфігурацію
- \`save_to_json()\` - Зберігає дані в JSON
- \`read_json()\` - Читає дані з JSON

### App (src/gui/app.py)

Головна GUI програма на базі CustomTkinter:

\`\`\`python
app = App(PARENT_DIR)
app.mainloop()
\`\`\`

---

## 🔍 Логування

Програма ведеться детальне логування всіх операцій. Логи включають:

- ✅ Успішні операції
- ⚠️ Попередження
- ❌ Помилки
- 🔧 Інформацію про конфігурацію

Логи виводяться в консоль та можуть бути збережені в файл.

---

## 🐛 Вирішення проблем

### Помилка: "ModuleNotFoundError: No module named 'instaloader'"

**Рішення:** Встановіть залежності:
\`\`\`bash
pip install instaloader customtkinter
\`\`\`

### Помилка: "Instagram login required"

**Рішення:** Деякі профілі вимагають авторизації. Переконайтеся, що профіль є публічним.

### Помилка: "Path not found"

**Рішення:** Перевірте, що папка призначення існує та у вас є права доступу.

---

## 📄 Ліцензія

Цей проект розповсюджується під ліцензією MIT.

---

## 👨‍💻 Розробка

### Структура коду

- **MVC паттерн** - Розділення моделі, представлення та контролера
- **Модульна архітектура** - Легко розширювана структура
- **JSON конфігурація** - Гнучкі налаштування без змін коду

### Розширення функціональності

Для додавання нових функцій:

1. Додайте логіку в \`src/insta_parser/core.py\`
2. Додайте GUI елементи в \`src/gui/windows/\`
3. Оновіть конфігурацію в \`config/\`

---

## 📞 Контакти та підтримка

Якщо у вас виникли питання або проблеми, будь ласка:

- Перевірте документацію [Instaloader](https://instaloader.github.io/)
- Перевірте документацію [CustomTkinter](https://github.com/TomSchimansky/CustomTkinter)
- Створіть Issue в репозиторії

---

## 🙏 Подяки

- [Instaloader](https://instaloader.github.io/) - Бібліотека для завантаження контенту
- [CustomTkinter](https://github.com/TomSchimansky/CustomTkinter) - Сучасний GUI фреймворк

---

**Версія:** 1.0 Beta 
**Останнє оновлення:** 2026
